# 隐私说明

ToApp Lite 的设计目标是“输入不离开设备”。

## 不收集

生成器不收集、不上传、不共享：

- 用户输入的网址、应用名或包名；
- 选择的图标；
- 生成的 APK；
- 签名私钥或 P12 密码；
- 设备标识、手机号、网络标识、位置或文件列表；
- 使用统计、崩溃日志或行为事件。

## 本地保存

生成器会在应用私有目录保存：

- RSA 私钥；
- 自签名 X.509 证书。

Android 的其他普通应用不能直接访问该目录。卸载应用或清除应用数据会删除这些文件，因此用户应主动导出 P12 备份。

## 网络

ToApp Lite 生成器不声明 `INTERNET` 权限，因此 Android 系统层面禁止其联网。

生成出来的网页应用需要网络权限加载用户指定的 HTTPS 网站。该网站自身的数据处理行为不受 ToApp Lite 控制，用户应只打包自己信任的网站。
